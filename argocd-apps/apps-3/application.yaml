apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-apps-4
  namespace: argocd
spec:
  project: default
  source:
    path: my-apps/apps-4
    repoURL: 'https://github.com/pintulohargcetts/argocd-application-example.git'
    targetRevision: HEAD
    helm:
      valueFiles:
        - values.yaml
      parameters:
        - name: installCRDs
          value: "true"
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: beta
  syncPolicy:
    automated: 
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true